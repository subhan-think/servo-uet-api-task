import React from "react";
import FormControl from "@mui/material/FormControl";

function FormControlWrapper({ children }) {
  return <FormControl>{children}</FormControl>;
}

export default FormControlWrapper;
